[project]
name = "py-caption-service"
version = "0.1.0"
description = "Small FastAPI service to fetch YouTube captions for the Next.js app"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "youtube-transcript-api",
]
